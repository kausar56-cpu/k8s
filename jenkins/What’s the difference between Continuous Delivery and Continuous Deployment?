“Continuous Delivery means every code change is automatically tested and ready for release, 
but the actual deployment to production needs manual approval. Continuous Deployment goes a 
step further — once tests pass, the code is automatically
deployed to production without human intervention.”
in Delivery the build waits for a manager’s approval before going live, but in Deployment it goes live immediately after passing tests
