If you're trying to print the date every second for 10 seconds, 
you should use the following correct Bash loop:

for i in {1..10}
do
  echo $(date)
  sleep 1
done
-------------------------------------------------------------------------------------------------------------------------------
for I in {1..10}; do 
    date | awk '{print $1, $2, $3, $4}';  
    sleep 1; 
done

-------------------------------------------------------------------------------------------------------------------------------------
variable dlceation

SG="KAUSAR"
echo $SG
hear we can give single or doble qutes
---------------------------------------------------------------------------
------------------------------------------------------------------------------------------

x=10  # Assign value 10 to variable x

SG="saikiran-ops-$x"  # Correct way to assign a string with variable expansion

echo $SG  # Print the value of SG
saikiran-ops-10
------------------------------------------------------------------------------------------------------------------------------------------

x=10  # Assign value 10 to variable x

SG='saikiran-ops-$x'  # Correct way to assign a string with variable expansion

echo $SG  # Print the value of SG
saikiran-ops-x
-----------------------------------------------------------------------------------------------------------------------------------------------------------

aws s3 ls | cut -d ' ' -f 2 > text.logs
test log mde 2no cha column janar 
----------------------------------------------------------------------------------------------------------------------------------------------------------------------------

rephrese word using awk
aws s3 ls | awk '{print $3, $1}'

output
cloudvishwakarma.in 2024-07-23
------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
for dibgging set -x we use
--------------------------------------------------------------------------------------------------------------------------------------------------------------------------




























